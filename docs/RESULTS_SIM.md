# Simulation Results (P6)

**Note:** All results produced by simulation in this document are on PLACEHOLDER `config/measured_params.yaml` parameters pending the real P1 hardware campaign. The current placeholder distances put nodes in the logistic curve's sensitive region differently than physical tests might, so simulated PDR is unrealistically flat. This will flatten Channel-Aware Greedy and Oracle advantages over Max-Weight.

## Main Sweep
- Results stubbed.

## Generalisation Battery
- Evaluated at `distances_m` [10.0, 20.0, 30.0, 40.0] as requested. Results stubbed.

## Statistics
- Shuffled-label control confirmed non-significant (p >= 0.05).
